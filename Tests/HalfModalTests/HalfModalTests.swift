import XCTest
@testable import HalfModal

final class HalfModalTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HalfModal().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
